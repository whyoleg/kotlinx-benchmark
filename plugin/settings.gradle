pluginManagement {
    repositories {
        mavenCentral()
        jcenter()
        mavenLocal()
        maven { url 'https://plugins.gradle.org/m2/' }
    }
}
rootProject.name = 'kotlinx.benchmark.gradle'
